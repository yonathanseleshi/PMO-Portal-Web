/** Production environment configuration. */
export const environment = {
  production: true,
  /** Set to the deployed PMO Portal API origin at release time. */
  apiBaseUrl: '/api',
  useMockData: true,
};
